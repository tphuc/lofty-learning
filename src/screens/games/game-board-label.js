import React from 'react';
import { ImageBackground } from 'react-native'

const GameBoardLabel = (props) => {
    return (
        <ImageBackground source={require('../../assets/images/game-board-holder.png')}>

        </ImageBackground>
    )
}

export default GameBoardLabel;